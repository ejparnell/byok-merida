# Include fit analysis with generated resume content

Superseded by ADR-0035.

The Job-Specific Resume page body includes both stable sections: `Resume Fit Analysis` first and `Job-Specific Resume` second. Keeping the analysis visible makes the generated resume auditable, shows which gaps were intentionally ignored, and gives future resume edits a traceable evidence map instead of only a polished final draft.
