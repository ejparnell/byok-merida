# Serve resume creation on separate page

The Resume creation interface lives at `/resumes` instead of being folded into `/analysis` or a broader dashboard. `/analysis` stays focused on producing Job Posting Analysis, while `/resumes` owns the small queue for creating the first Job-Specific Resume from ready analyzed Job Postings.
