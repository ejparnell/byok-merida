# Use relation as resume existence signal

Creating a Job-Specific Resume does not update a separate status or checkbox on the related Job Posting. The two-way Notion relation is the only durable v1 signal that a Resume exists for a Job Posting, avoiding a second source of truth before the workflow needs additional resume lifecycle states.
