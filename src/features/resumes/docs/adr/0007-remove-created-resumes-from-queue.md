# Remove created resumes from queue

After a Job-Specific Resume is successfully generated and attached, the `/resumes` page removes the related Job Posting from the visible Resume Creation Queue and shows the created Resume link in the result message. The queue removal follows from the relation being the durable proof that a Resume exists, while the link lets the user jump directly to the new Notion Resume when they want to inspect it.
