# Merida Handoff - Application-Ready Resume Draft Deepening

## Context

This session continued the architecture cleanup from:

- `report/architecture-review-20260708140205.html`
- Prior handoff: `report/merida-handoff-20260708150133.md`

The user asked to correct the architecture report item:

- `Deepen Application-Ready Resume Draft assembly`

The user specifically asked to use the codebase-design skill vocabulary while improving the code format. No secrets, API keys, passwords, live Notion payloads, or personal contact details are included in this handoff.

## Current State

The architecture report's Application-Ready Resume Draft recommendation has been implemented.

New module:

- `src/features/resumes/lib/applicationReadyResumeDraft.js`

Updated orchestration:

- `src/features/resumes/backend/resumeService.js`

New focused tests:

- `src/features/resumes/test/applicationReadyResumeDraft.test.js`

Updated narrower service tests:

- `src/features/resumes/test/resumeService.test.js`

New ADR:

- `src/features/resumes/docs/adr/0034-deepen-application-ready-resume-draft-module.md`

The new design shape is:

- `ApplicationReadyResumeDraft` is a feature-owned module with one external interface: `create({ resumeName, jobPosting, masterEvidenceItems, fitScore })`.
- The module owns Master Resume role skeleton extraction, fixed-template role validation, the injected resume-generation adapter call, Resume Claim Trace repair, work-experience role completion, bullet-count enforcement, and Notion block rendering.
- Resume Creation now stays focused on workflow sequencing: validate/read Notion source pages, run Resume Fit Analysis, ask Application-Ready Resume Draft for blocks, create an unlinked draft, append blocks, then attach the relation.
- Route names and the workflow surface remain stable: `/resumes`, `/resumes/status`, and `/resumes/create`.

## Verification

Focused verification passed:

- `node --test src/features/resumes/test/*.test.js` - 41 Node tests
- `python3 -m unittest discover -s src/features/resumes/ml/test` - 8 Python tests
- `node --test src/features/resumes/test/applicationReadyResumeDraft.test.js src/features/resumes/test/resumeService.test.js` - 21 Node tests

Full verification passed outside the sandbox:

- `npm test` - 95 Node tests plus 8 Python tests

Important test environment note:

- Sandboxed `npm test` still hits the known `listen EPERM: operation not permitted 127.0.0.1` failure in `src/backend/test/server.test.js`.
- The same `npm test` command passed outside the sandbox with approved localhost binding.

## Operational Notes

If the user retries live Create Resume after recent Resumes runtime changes, restart any running `npm start` process first. The Python Resume Fit Analysis runtime is long-running and will not pick up Python runtime changes until restart.

The current session did not include a live `/resumes/create` run against Notion. It only changed local code and tests.

## Likely Next Work

If continuing the architecture review cleanup, the next visible item after Application-Ready Resume Draft in `report/architecture-review-20260708140205.html` is:

- `Deepen Capture Evidence`

Before implementing the next item, read the report section and nearby Job Postings code. The user's current IDE tabs are in backend route/config files, so the next session may involve backend seam cleanup around routes, config, contracts, or server composition.

If the user instead wants to retry Create Resume:

- Ask whether `npm start` was restarted after the Resume Fit Analysis runtime fix.
- If Create Resume still fails, build a tight failing loop from the live failing job/resume payload before changing thresholds.
- Inspect actual Fit Requirements and runtime candidates with temporary tagged instrumentation or a local replay fixture.
- Suspect skill dictionary gaps, overly broad LLM requirements, or true evidence mismatch before relaxing guardrails.

## Suggested Skills

- `docs/skills/engineering/codebase-design/SKILL.md` - use for any further module/interface/seam/depth refactor.
- `docs/skills/engineering/improve-codebase-architecture/SKILL.md` - use when continuing through `report/architecture-review-20260708140205.html`.
- `docs/skills/engineering/tdd/SKILL.md` - use for regression-first changes around draft assembly, capture evidence, or backend routing.
- `docs/skills/engineering/diagnosing-bugs/SKILL.md` - use if live Create Resume or backend server behavior is failing.
- `docs/skills/engineering/domain-modeling/SKILL.md` - use only if new Resumes or Job Postings vocabulary/ADRs are needed.
- `docs/skills/productivity/handoff/SKILL.md` - use for the next handoff.
